            <div class="col-span-12 lg:col-span-3 2xl:col-span-2 flex lg:block  ">

                <div class="intro-y box mt-3  w-full" style="direction:ltr">
                    <div class="relative flex items-center py-4 p-2">
                        <div class="w-12 h-12 image-fit">
                            <img src="{{app.CONF.url}}image?src=rocket.png&w=200" class="rounded-full" />
                        </div>
                        <div class="ml-4 mr-auto">
                            <div class="font-medium text-base">{{app.Customer.name}}</div>
                            <div class="text-slate-500">{{app.Customer.email}}</div>
                        </div>
                    </div>
                    <div class=" bg-white my-2  lg:mr-4 py-3 p-2 border-t  text-base font-semibold border-slate-200/60 dark:border-darkmode-400 text-gray-600">
                        <a class="flex items-center mt-4 text-left" href="{{app.CONF.url}}customer/profile">
                            <i data-feather="activity" class="w-4 h-4 mr-2"></i> {{app.Lang.PERS_INFO}}
                        </a>
                        <a class="flex items-center mt-4" href="{{app.CONF.url}}customer/address">
                            <i data-feather="box" class="w-4 h-4 mr-2"></i> {{app.Lang.LOCATIONS}}
                        </a>
                        <a class="flex items-center mt-4" href="{{app.CONF.url}}customer/projects">
                            <i data-feather="activity" class="w-4 h-4 mr-2"></i> {{app.Lang.PROJECTS}}
                        </a>
                        <a class="flex items-center mt-4" href="{{app.CONF.url}}customer/orders">
                            <i data-feather="box" class="w-4 h-4 mr-2"></i> {{app.Lang.ORDERS}}
                        </a>
                        <a class="flex items-center mt-4" href="">
                            <i data-feather="lock" class="w-4 h-4 mr-2"></i> Social Networks
                        </a>
                    </div>
                    <div class="p-5 border-t border-slate-200/60 dark:border-darkmode-400 flex">
                        <a href="{{app.CONF.url}}logout" class="btn btn-outline-secondary py-3 px-5 mx-auto">{{app.Lang.LOGOUT}}</a>
                    </div>
                </div>
            </div>
